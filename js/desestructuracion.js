// acceder a un array

const {
  data: { movies: pelis },
} = 3;
// data set
// en el css data-id="${}" data-category=
// $element.dataset.id();
/**
 * Buscando elementos en lista
 * findMovie(id, category) {
 *  array.find();
 * }
 * simplificando arrow functions
 * (() => item) evitando return
 *
 * animaciones
 * .fadeIn {
 *  animation: 1000ms fadeIn
 * }
 * @keyFrames fadeIn {
 * 0% {
 * opacity: 0
 * }
 * 100% {
 * opacity: 1
 * }
 * }
 * const image = movieElement;
 * image.addEventListener("load", () => {
 * event.srcElement.classList.add("fadeIn");
 * })
 * renderizando peticiones
 *
 * manejo de errores
 * if(data.data.movie_count > 0) {
 *  return data;
 * }
 * try {
 * }catch(error.message) {
 *
 * }
 * throw new Error("No se encontro ningun resultado");
 *
 * LocalStorage
 * window.localStorage.setItem('nombre', JSON.sringifu({'peli':wonderwoman}))
 * JSON.parse(window.localStorage.setItem('actionList', JSON.stringify(actionList));
 * async function cacheExists() {
 *  const cache = window.localStorage.getItem("actionList")
 *  devuelve null
 *  return JSON.parse(cacheList);
 *  return await getData();
 *
 * }
 */
